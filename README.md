# VisionWatchApp public configuration

Set the placeholders in `app/src/main/java/com/example/visionwatchapp/ApiConfig.kt` for your own service credentials. The private SDK binaries under `app/libs` are intentionally excluded; provide them locally before building.
